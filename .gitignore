# Python
__pycache__/
*.pyc

# Virtual environments
venv/
.venv/

# Project data (regenerated by the pipeline; not shipped)
data/raw/
data/processed/
# Tennis history (Sackmann) is non-commercial (CC BY-NC-SA) -- never commit it.
data/tennis/

# Secrets — never commit real keys (the .env.example template is kept)
.env
.env.*
!.env.example

# Editors / OS junk
.vscode/
.idea/
.DS_Store
Thumbs.db

# Packaging / Supabase CLI / misc
*.zip
supabase/.temp/
